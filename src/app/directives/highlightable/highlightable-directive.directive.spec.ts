import { HighlightableDirectiveDirective } from './highlightable-directive.directive';

describe('HighlightableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
